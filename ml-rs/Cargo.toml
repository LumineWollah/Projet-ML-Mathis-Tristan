[package]
name = "ml_rs"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Minimal Rust ML library (snake_case variables): Perceptron (Rosenblatt) + Linear Regression (delta rule)"

[lib]
name = "ml_rs"
path = "src/lib.rs"

[dependencies]
rand = "0.8"
